import 'package:flutter/material.dart';

//TODO implement functionality to save movies and to view them

class SavedPage extends StatelessWidget {
  const SavedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Saved movies:'),
      ],
    );
  }
}
